# FreshKeep backend runtime dependencies.
#
# The canonical dependency list lives in pyproject.toml; this file mirrors it
# with pinned versions for a quick `pip install -r requirements.txt` setup.
#
#   python3 -m venv .venv
#   ./.venv/bin/pip install -r requirements.txt          # runtime only
#   ./.venv/bin/pip install -r requirements-dev.txt      # + test tooling
#
# (Equivalent to `pip install -e ".[dev]"`, which is the recommended path.)

fastapi==0.139.0
uvicorn[standard]==0.49.0
sqlmodel==0.0.39
pydantic-settings==2.14.2
python-multipart==0.0.32
anthropic==0.116.0
google-genai==2.10.0
